# Google AI API Key - Get yours at https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Application configuration
NODE_ENV=development # Use 'development' for local dev, 'production' for deployment
PORT=9002 # Port for the Next.js application (automatically set to 9002 in package.json)

# Database Service Configuration 
DB_SERVER_PORT=3100 # Port for the database service
NEXT_PUBLIC_API_URL=http://localhost:3100 # URL for client-side API calls to the database service

# Docker specific (these are set in the Dockerfile/docker-compose.yml, you don't need to set them locally)
# NODE_ENV=production
# NEXT_PUBLIC_API_URL=http://localhost:3100
