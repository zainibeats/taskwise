# Google AI API Key - Get yours at https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# -----------------------------------------
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# -----------------------------------------
# Use these settings when developing locally
NODE_ENV=development
PORT=9002 # Port for the Next.js application in development mode

# Development database service configuration 
DB_SERVER_PORT=3100 # Port for the database service
NEXT_PUBLIC_API_URL=http://localhost:3100 # For local development only

# -----------------------------------------
# SELF-HOSTING PRODUCTION CONFIGURATION
# -----------------------------------------
# When self-hosting in production with Docker, the main settings are configured in:
# 1. docker-compose.yml - For the API URL (as a build arg)
# 2. Through environment variables for the Google AI API key

# IMPORTANT: For self-hosting, you must set NEXT_PUBLIC_API_URL to your server's
# actual IP address or hostname in docker-compose.yml before building:
#
# services:
#   taskwise:
#     build:
#       args:
#         - NEXT_PUBLIC_API_URL=http://your-server-ip:3100
